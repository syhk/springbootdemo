#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../_nanoid@3.3.4@nanoid/bin/nanoid.cjs" "$@"
else 
  exec node  "$basedir/../_nanoid@3.3.4@nanoid/bin/nanoid.cjs" "$@"
fi
