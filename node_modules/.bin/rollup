#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../_rollup@2.78.1@rollup/dist/bin/rollup" "$@"
else 
  exec node  "$basedir/../_rollup@2.78.1@rollup/dist/bin/rollup" "$@"
fi
